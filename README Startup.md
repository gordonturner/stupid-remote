README Startup
==============

- Make sure to install node dependencies:

```
npm install
```

- From the root of the project, run:

```
cd stupid-remote
node ./bin/www
```


- Debug startup:

```
cd stupid-remote
DEBUG=* node ./bin/www
```

